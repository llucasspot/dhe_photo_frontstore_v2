import { enAuth } from '../../../../../../auth/i18n';

import { Translations } from '#i18n/domain';

export const en: Translations = {
  auth: enAuth,
  home: {
    title: 'Home',
    subtitle: 'home',
  },
} as const;
